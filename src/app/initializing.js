import chalk from 'chalk';
import yosay from 'yosay';

export default function initializing() {
  this.log(yosay('Create your own Universal React App with ' + chalk.blue('RentPath\'s') + ' choice of fine technologies!'));
}
